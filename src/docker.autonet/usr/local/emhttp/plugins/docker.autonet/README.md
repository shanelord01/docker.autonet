**Docker Autonet**

Connects Docker containers to Docker networks automatically, based on container labels. Configure a label key to network mapping under **Settings -> Docker Autonet**, then tag containers with that label - through the **Add Labels** button on the Docker page, a template, or a compose file - and reconciliation connects them on the next pass.
